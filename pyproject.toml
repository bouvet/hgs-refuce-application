[project]
name = "hgs-refuce-application"
version = "0.1.0"
description = "Backend skeleton for hgs-refuce-application"
requires-python = ">=3.10"

dependencies = [
  "fastapi",
  "uvicorn[standard]",
]

[tool.pytest.ini_options]
addopts = "-q"
